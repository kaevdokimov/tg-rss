# Настройка отправки отчетов в Telegram

## Автоматическая отправка всем пользователям

Отчеты автоматически отправляются **всем пользователям**, которые подписаны на бота (находятся в таблице `users` в БД).

## Настройка

### GitHub Actions (рекомендуется)

Токен бота для отправки отчетов настраивается через GitHub Actions secrets:

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секрет `TELEGRAM_SIGNAL_API_KEY` с токеном бота для отчетов
3. При деплое токен автоматически передается в контейнер через `.env` файл

**Важно**: Это отдельный бот от основного бота (`TELEGRAM_API_KEY`). Создайте нового бота через @BotFather для отправки отчетов.

### Локальная разработка

Для локальной разработки добавьте в `.env` (в корне проекта):
```env
TELEGRAM_SIGNAL_API_KEY=your_signal_bot_token_here
```

## Как это работает

1. **Получение пользователей**: После анализа система получает всех пользователей из таблицы `users` в БД
2. **Отправка отчетов**: Отчет отправляется каждому пользователю отдельным сообщением
3. **Обработка ошибок**: Если пользователь заблокировал бота или произошла ошибка, она логируется, но не останавливает отправку другим пользователям

## Формат отчета

Отчет отправляется в следующем формате:

```
📊 КАРТА ДНЯ - 15.12.2024 12:00

📰 Всего новостей: 150
🎯 Выявлено тем: 5

ТОП-ТЕМЫ ДНЯ:

1. Тема #0 (25 новостей)
🔑 Ключевые слова: война, конфликт, переговоры
📰 Примеры:
  • Заголовок 1...
  • Заголовок 2...

2. Тема #1 (20 новостей)
...
```

## Статистика отправки

После каждой отправки в логах отображается статистика:
- Количество успешно отправленных отчетов
- Количество ошибок
- Список chat_id, которым не удалось отправить (первые 10)

Пример лога:
```
Отправка завершена: успешно 45, ошибок 2
Не удалось отправить 2 пользователям: [123456789, 987654321]
```

## Обработка ошибок

### Пользователь заблокировал бота (403)
- Логируется как предупреждение
- Не влияет на отправку другим пользователям
- В статистике считается как ошибка

### Некорректный chat_id (400)
- Логируется как предупреждение
- Не влияет на отправку другим пользователям

### Другие ошибки
- Логируются с полной информацией
- Не останавливают отправку другим пользователям

## Проверка работы

### Проверка переменных окружения

```bash
# Проверить, что TELEGRAM_SIGNAL_API_KEY доступен в контейнере
docker exec -it news-analyzer env | grep TELEGRAM_SIGNAL_API_KEY
```

### Тестовая отправка

```bash
# Запустить анализ вручную (отчеты будут отправлены всем пользователям)
make analyzer-run

# Проверить логи
make analyzer-logs | grep -i telegram
```

### Проверка пользователей в БД

```bash
# Подключиться к БД
docker exec -it db psql -U postgres -d news_bot

# Проверить пользователей
SELECT chat_id, username FROM users;
```

## Настройка отдельного бота для отчетов

Рекомендуется использовать **отдельного бота** для отправки отчетов:

1. Создайте нового бота через @BotFather в Telegram
2. Получите токен бота
3. Добавьте токен в GitHub Actions secrets как `TELEGRAM_API_KEY`
4. Пользователи должны сначала написать боту `/start`, чтобы он мог им отправлять сообщения

**Важно**: Бот должен иметь возможность отправлять сообщения пользователям. Пользователи должны сначала написать боту, чтобы он мог им отправлять сообщения.

## Устранение проблем

### Отчеты не отправляются

1. **Проверьте TELEGRAM_SIGNAL_API_KEY:**
   ```bash
   docker exec -it news-analyzer env | grep TELEGRAM_SIGNAL_API_KEY
   ```
   Должен быть установлен токен бота для отчетов.

2. **Проверьте наличие пользователей:**
   ```bash
   docker exec -it db psql -U postgres -d news_bot -c "SELECT COUNT(*) FROM users;"
   ```

3. **Проверьте логи:**
   ```bash
   make analyzer-logs | grep -i telegram
   ```

### Пользователи не получают отчеты

1. **Пользователь должен написать боту**: Бот может отправлять сообщения только тем пользователям, которые сначала написали ему
2. **Проверьте, что пользователь в БД**: Убедитесь, что пользователь есть в таблице `users`
3. **Проверьте логи**: Ищите ошибки для конкретного chat_id

### Ошибка "bot was blocked by the user"

Это нормально - некоторые пользователи могут заблокировать бота. Такие ошибки логируются, но не влияют на отправку другим пользователям.

## Примеры использования

### Отправка только активным пользователям

Текущая реализация отправляет всем пользователям из таблицы `users`. Если нужно фильтровать пользователей, можно:

1. Добавить поле `active` в таблицу `users`
2. Модифицировать метод `get_all_users()` для фильтрации
3. Или создать отдельную таблицу подписок на отчеты

### Отправка в группу

Если нужно отправлять в группу вместо личных сообщений:

1. Добавьте бота в группу
2. Узнайте ID группы через @userinfobot
3. Модифицируйте код для отправки в группу (используйте отрицательный chat_id)

## Безопасность

- Токен бота хранится в GitHub Actions secrets
- Токен не попадает в код или логи
- При локальной разработке используйте `.env` файл (не коммитьте его в git)
