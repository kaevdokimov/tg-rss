#!/usr/bin/env python3
"""Скрипт для валидации конфигурации news-analyzer."""

import sys
from pathlib import Path

# Добавляем src в путь для импортов
sys.path.insert(0, str(Path(__file__).parent / "src"))

from src.config import print_config_validation_report


def main():
    """Основная функция валидации конфигурации."""
    print("🔍 Валидация конфигурации news-analyzer-python")
    print("=" * 60)

    try:
        # Выполняем валидацию
        is_valid = print_config_validation_report()

        print("\n" + "=" * 60)

        if is_valid:
            print("✅ Конфигурация готова к использованию!")
            return 0
        else:
            print("❌ Конфигурация требует исправлений!")
            print("\n💡 Советы по исправлению:")
            print("  1. Проверьте правильность всех обязательных полей")
            print("  2. Убедитесь, что числовые значения в допустимых диапазонах")
            print("  3. Проверьте корректность путей к директориям")
            print("  4. Учитывайте предупреждения для оптимизации производительности")
            return 1

    except Exception as e:
        print(f"❌ Ошибка при валидации конфигурации: {e}")
        return 1


if __name__ == "__main__":
    sys.exit(main())