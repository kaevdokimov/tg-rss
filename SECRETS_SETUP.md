# Настройка GitHub Secrets для News Analyzer API

## 📋 Обзор

Для защиты документации FastAPI (`/docs` и `/redoc`) необходимо добавить два новых секрета в GitHub репозиторий:

- `NEWS_ANALYZER_ADMIN` - имя пользователя для Basic Auth
- `NEWS_ANALYZER_PASSWORD` - пароль для Basic Auth

## 🔐 Добавление Secrets в GitHub

### Способ 1: Через веб-интерфейс GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте первый секрет:
   - **Name:** `NEWS_ANALYZER_ADMIN`
   - **Value:** ваше имя пользователя (например: `admin`)
5. Нажмите **Add secret**
6. Повторите для второго секрета:
   - **Name:** `NEWS_ANALYZER_PASSWORD`
   - **Value:** надежный пароль (минимум 16 символов)
7. Нажмите **Add secret**

### Способ 2: Через GitHub CLI

```bash
# Установите GitHub CLI если еще не установлен
# brew install gh (macOS)
# apt install gh (Ubuntu/Debian)

# Авторизуйтесь
gh auth login

# Добавьте secrets
gh secret set NEWS_ANALYZER_ADMIN -b"admin"
gh secret set NEWS_ANALYZER_PASSWORD -b"your_secure_password_here"
```

## 📝 Где используются эти secrets

### 1. GitHub Actions CI/CD Pipeline

Secrets автоматически передаются в контейнер при деплое через файлы:

- `.github/workflows/ci-cd.yml` (строки 327-328, 364-365)
  ```yaml
  # News Analyzer API Security
  NEWS_ANALYZER_ADMIN=${{ secrets.NEWS_ANALYZER_ADMIN }}
  NEWS_ANALYZER_PASSWORD=${{ secrets.NEWS_ANALYZER_PASSWORD }}
  ```

### 2. Docker Compose

В `docker-compose.yml` secrets передаются как переменные окружения:

```yaml
news-analyzer:
  environment:
    NEWS_ANALYZER_ADMIN: ${NEWS_ANALYZER_ADMIN:-admin}
    NEWS_ANALYZER_PASSWORD: ${NEWS_ANALYZER_PASSWORD:-changeme}
```

### 3. Локальная разработка

Для локальной разработки добавьте в `.env` файл:

```bash
# News Analyzer API Security
NEWS_ANALYZER_ADMIN=admin
NEWS_ANALYZER_PASSWORD=your_local_password
```

## 🚀 Проверка настройки

### После добавления secrets в GitHub

1. Сделайте коммит и push в `main` branch
2. GitHub Actions автоматически задеплоит обновления
3. После деплоя проверьте доступ:

```bash
# Без авторизации - должен вернуть 401
curl http://your-server:8000/docs

# С правильными учетными данными - должен вернуть HTML
curl -u admin:your_password http://your-server:8000/docs
```

### Локальная проверка

```bash
# Запуск контейнера локально
docker-compose up -d news-analyzer

# Проверка health
curl http://localhost:8000/health

# Проверка защищенной документации
curl -u admin:changeme http://localhost:8000/docs
```

## 🔒 Рекомендации по безопасности

### Генерация надежного пароля

```bash
# Генерация случайного пароля (32 символа)
openssl rand -base64 32

# Или через pwgen (если установлен)
pwgen -s 32 1
```

### Best Practices

1. **Используйте сильные пароли**
   - Минимум 16 символов
   - Комбинация букв, цифр и спецсимволов
   - НЕ используйте дефолтные значения (`admin`/`changeme`) в production

2. **Регулярно меняйте пароли**
   - Обновляйте пароль минимум раз в 90 дней
   - При подозрении на компрометацию - немедленно

3. **Ограничьте доступ**
   - Используйте firewall для ограничения доступа к порту 8000
   - Настройте VPN если возможно
   - Рассмотрите использование IP whitelist

4. **Мониторинг**
   - Включите логирование попыток доступа
   - Настройте alerts на множественные неудачные попытки

## 🔄 Обновление существующих secrets

### Через веб-интерфейс

1. **Settings** → **Secrets and variables** → **Actions**
2. Найдите нужный secret в списке
3. Нажмите **Update**
4. Введите новое значение
5. Нажмите **Update secret**

### Через GitHub CLI

```bash
# Обновление секрета
gh secret set NEWS_ANALYZER_PASSWORD -b"new_secure_password"
```

### После обновления

После обновления secrets необходимо передеплоить приложение:

```bash
# Способ 1: Через push в main branch
git commit --allow-empty -m "chore: trigger redeploy for updated secrets"
git push origin main

# Способ 2: Ручной запуск workflow
gh workflow run ci-cd.yml
```

## 🐛 Troubleshooting

### Проблема: /docs возвращает 401 даже с правильным паролем

**Причина:** Secrets не обновились на сервере

**Решение:**
```bash
# 1. Проверьте что secrets добавлены в GitHub
gh secret list | grep NEWS_ANALYZER

# 2. Принудительный редеплой
git commit --allow-empty -m "chore: redeploy"
git push origin main

# 3. Проверьте логи контейнера
docker logs news-analyzer

# 4. Проверьте переменные окружения в контейнере
docker exec news-analyzer env | grep NEWS_ANALYZER
```

### Проблема: Secrets не видны в GitHub Actions

**Причина:** Недостаточно прав или secrets добавлены в неправильное место

**Решение:**
- Убедитесь что secrets добавлены в **Repository secrets**, а не в Environment secrets
- Проверьте что у вас есть права администратора на репозиторий
- Secrets должны быть видны в Settings → Secrets and variables → Actions

### Проблема: Дефолтные значения используются вместо secrets

**Причина:** Secrets не передаются в .env файл или docker-compose

**Решение:**
1. Проверьте `.github/workflows/ci-cd.yml` - убедитесь что secrets добавлены в оба места создания .env
2. Проверьте `docker-compose.yml` - убедитесь что переменные добавлены в environment
3. Проверьте что на сервере файл `.env` содержит правильные значения:
   ```bash
   ssh user@server "cat ~/news-bot/.env | grep NEWS_ANALYZER"
   ```

## 📚 Связанные файлы

- `docker-compose.yml` - конфигурация Docker
- `.github/workflows/ci-cd.yml` - CI/CD pipeline
- `news-analyzer-python/src/monitoring/api.py` - реализация Basic Auth
- `news-analyzer-python/API_SECURITY_README.md` - техническая документация
- `.env.example` - пример конфигурации
- `env.example` - корневой пример конфигурации

## ✅ Checklist

После настройки secrets проверьте:

- [ ] Secrets добавлены в GitHub (Settings → Secrets and variables → Actions)
- [ ] `.github/workflows/ci-cd.yml` обновлен с новыми переменными
- [ ] `docker-compose.yml` обновлен с новыми environment variables
- [ ] `.env` и `env.example` обновлены
- [ ] Сделан коммит и push изменений
- [ ] GitHub Actions успешно завершился
- [ ] Приложение задеплоено на сервер
- [ ] `/docs` требует авторизацию (возвращает 401 без пароля)
- [ ] `/docs` доступна с правильным паролем
- [ ] Пароли надежные (не дефолтные значения)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи GitHub Actions
2. Проверьте логи контейнера: `docker logs news-analyzer`
3. Проверьте документацию: `news-analyzer-python/API_SECURITY_README.md`
