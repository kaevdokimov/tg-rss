---
- name: Настройка сервера для TG-RSS приложения
  hosts: tg_rss_servers
  become: yes
  gather_facts: yes

  vars:
    # Защита данных
    create_backups: true
    backup_retention_days: 7

    # Настройки swap
    swap_file_size: "1G"
    swap_file_path: "/swapfile"

    # Настройки Docker
    docker_daemon_config:
      storage-driver: "overlay2"
      log-driver: "json-file"
      log-opts:
        max-size: "10m"
        max-file: "3"
      default-ulimits:
        nofile:
          soft: 65536
          hard: 65536
      iptables: false
      bridge: "none"
      ip-masq: false
      userland-proxy: false

    # Системные оптимизации для ограниченных ресурсов
    sysctl_params:
      # Swap
      vm.swappiness: 60
      vm.vfs_cache_pressure: 50

      # Network
      net.core.somaxconn: 1024
      net.ipv4.tcp_tw_reuse: 1
      net.ipv4.ip_local_port_range: "1024 65535"

      # Memory
      vm.overcommit_memory: 1
      vm.overcommit_ratio: 50

      # Filesystem
      fs.file-max: 2097152

  pre_tasks:
    - name: Полная очистка Docker перед настройкой сервера
      shell: |
        set -e
        echo "🗑️ Полная очистка Docker..."

        # Останавливаем и удаляем все Docker сервисы
        systemctl stop docker docker.socket containerd || true
        systemctl disable docker docker.socket containerd || true

        # Удаляем все Docker пакеты
        apt-get remove --purge -y docker docker-engine docker.io containerd runc \
          docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin || true

        # Удаляем все Docker repository файлы
        rm -f /etc/apt/sources.list.d/docker*.list
        rm -f /usr/share/keyrings/docker*.gpg
        rm -f /etc/apt/trusted.gpg.d/docker*.gpg

        # Очищаем apt cache
        apt-get clean
        apt-get autoclean
        rm -rf /var/lib/apt/lists/*docker*
        rm -rf /var/lib/docker
        rm -rf /etc/docker

        echo "✅ Docker полностью очищен"
      args:
        executable: /bin/bash
      ignore_errors: true
      when: ansible_os_family == "Debian"

    - name: Проверка и перезапуск SSH сервиса
      service:
        name: ssh
        state: restarted
      ignore_errors: true
      when: ansible_os_family == "Debian"

    - name: Обновление списка пакетов
      apt:
        update_cache: yes
        cache_valid_time: 3600
      when: ansible_os_family == "Debian"

  roles:
    - system_optimization
    - docker_setup
    - monitoring_setup
    - security_hardening
