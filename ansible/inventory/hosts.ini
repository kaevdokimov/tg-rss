# Этот файл используется ТОЛЬКО для локального тестирования Ansible
# В CI/CD inventory создается динамически с реальными секретами
#
# Пример конфигурации для локального тестирования:
# [tg_rss_servers]
# your-server-ip ansible_host=YOUR_SERVER_IP ansible_user=your_user
#
# [all:vars]
# ansible_python_interpreter=/usr/bin/python3
# ansible_ssh_common_args='-o StrictHostKeyChecking=no'
