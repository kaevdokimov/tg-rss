# 🔐 Настройка SSH для CI/CD

Полная инструкция по настройке SSH подключения для GitHub Actions CI/CD.

## 🚀 Быстрый старт

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
# Запустите скрипт автоматической настройки
./fix-ssh-setup.sh

# Следуйте инструкциям скрипта
# Скрипт сгенерирует ключ, добавит на сервер и покажет GitHub Secrets
```

### Вариант 2: Ручная настройка

#### 1. Сгенерируйте SSH ключ
```bash
# Создайте новый SSH ключ для GitHub CI
ssh-keygen -t ed25519 -C "github-ci@your-domain.com" -f ~/.ssh/github_ci

# Или используйте RSA (если ed25519 не поддерживается)
ssh-keygen -t rsa -b 4096 -C "github-ci@your-domain.com" -f ~/.ssh/github_ci
```

### 2. Протестируйте ключ локально
```bash
# Запустите наш скрипт тестирования
./test-ssh-key.sh ~/.ssh/github_ci

# Должен показать:
# ✅ Формат: OpenSSH (современный)
# ✅ Ключ валиден: 256 SHA256:... github-ci@your-domain.com
```

### 3. Настройте сервер
```bash
# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_ci.pub user@your-server

# Или вручную добавьте в ~/.ssh/authorized_keys
cat ~/.ssh/github_ci.pub | ssh user@server "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### 4. Настройте GitHub Secrets
```bash
# Скопируйте приватный ключ
cat ~/.ssh/github_ci

# В GitHub: Settings → Secrets and variables → Actions
# Создайте секрет: SERVER_SSH_KEY
# Вставьте содержимое приватного ключа
```

### 5. Настройте остальные секреты
```bash
# SERVER_HOST - IP адрес или домен сервера
# SERVER_USER - имя пользователя на сервере
# SERVER_PORT - порт SSH (по умолчанию 22)
```

## 🔍 Диагностика проблем

### 1. Используйте скрипт исправления
```bash
# Быстрое исправление всех SSH проблем
./fix-ssh-setup.sh
```

### 2. Запустите диагностический workflow
1. Перейдите в **GitHub Actions**
2. Выберите **SSH Diagnostic** workflow
3. Нажмите **Run workflow**
4. Проверьте логи выполнения

### Распространенные проблемы

#### ❌ "Load key error in libcrypto"
**Причина:** Поврежденный или неправильный формат ключа
**Решение:**
```bash
# Проверьте ключ
./test-ssh-key.sh ~/.ssh/github_ci

# Сгенерируйте новый ключ
ssh-keygen -t ed25519 -C "github-ci" -f ~/.ssh/github_ci_new
```

#### ❌ "Permission denied (publickey,password)"
**Причина:** Публичный ключ не добавлен на сервер
**Решение:**
```bash
# Добавьте ключ на сервер
ssh-copy-id -i ~/.ssh/github_ci.pub user@server

# Проверьте подключение локально
ssh -i ~/.ssh/github_ci user@server "echo 'SSH works'"
```

#### ❌ "Invalid format" или "PKCS#8"
**Причина:** Ключ в неподдерживаемом формате
**Решение:**
```bash
# Конвертируйте ключ
ssh-keygen -p -f ~/.ssh/github_ci -m pem

# Или сгенерируйте новый
ssh-keygen -t ed25519 -C "github-ci" -f ~/.ssh/github_ci
```

## 🔧 Ручная проверка

### Проверьте формат ключа
```bash
# Должен начинаться с:
# -----BEGIN OPENSSH PRIVATE KEY-----

head -1 ~/.ssh/github_ci
```

### Проверьте валидность ключа
```bash
ssh-keygen -l -f ~/.ssh/github_ci
```

### Протестируйте подключение
```bash
# С правильным портом, если отличается от 22
ssh -i ~/.ssh/github_ci -p 22 user@server "echo 'Success'"
```

## 📋 Требования к SSH ключу

- ✅ **Формат**: OpenSSH (начинается с `-----BEGIN OPENSSH PRIVATE KEY-----`)
- ✅ **Тип**: Ed25519 или RSA 4096+
- ✅ **Права**: 600 (`chmod 600 ~/.ssh/github_ci`)
- ✅ **Без пароля**: Ключ без парольной фразы
- ✅ **Добавлен на сервер**: Публичный ключ в `~/.ssh/authorized_keys`

## 🚨 Безопасность

- 🔐 **Никогда не коммитьте приватные ключи** в репозиторий
- 🔐 **Используйте отдельный ключ** только для CI/CD
- 🔐 **Регулярно ротируйте ключи**
- 🔐 **Ограничьте доступ** к GitHub Secrets

## 🎯 Следующие шаги

После настройки SSH:
1. ✅ **Запустите SSH Diagnostic** workflow для проверки
2. ✅ **Запустите основной CI/CD** pipeline
3. ✅ **Проверьте логи** на наличие ошибок
4. ✅ **Настройте мониторинг** после успешного деплоя

**SSH ключ настроен правильно - CI/CD заработает!** 🚀✨
