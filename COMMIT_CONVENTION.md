# Соглашение о сообщениях коммитов

Проект использует **Conventional Commits** для стандартизации сообщений коммитов на русском языке.

## Формат сообщения коммита

```
<тип>(<область>): <краткое описание>

<подробное описание>

<ссылки на задачи>
```

## Типы коммитов

| Тип | Описание | Пример |
|-----|----------|---------|
| **feat** | Новая функциональность | `feat(bot): добавить поддержку inline-кнопок` |
| **fix** | Исправление ошибки | `fix(db): исправить утечку null байтов` |
| **docs** | Изменения в документации | `docs(readme): обновить инструкцию по установке` |
| **style** | Форматирование кода (без функциональных изменений) | `style(scraper): исправить отступы в функциях` |
| **refactor** | Рефакторинг кода | `refactor(redis): оптимизировать структуру кэша` |
| **test** | Добавление или исправление тестов | `test(scraper): добавить тесты для очистки null байтов` |
| **chore** | Служебные изменения (зависимости, конфигурация) | `chore(docker): обновить версию базового образа` |
| **perf** | Улучшение производительности | `perf(cache): добавить Redis кэширование контента` |
| **ci** | Изменения в CI/CD | `ci(github): добавить тесты производительности` |
| **build** | Изменения в системе сборки | `build(go): обновить зависимости модулей` |

## Область (scope)

Область указывает на часть проекта, которую затрагивает изменение:

- **bot** - логика Telegram бота
- **scraper** - парсинг контента новостей
- **rss** - обработка RSS лент
- **redis** - Redis кэширование и pub/sub
- **db** - работа с базой данных
- **config** - конфигурация приложения
- **monitoring** - метрики и логирование
- **docker** - контейнеризация
- **ci** - непрерывная интеграция
- **docs** - документация
- **test** - тестирование

## Краткое описание

- **Обязательно** на русском языке
- **Начинается с маленькой буквы**
- **Не заканчивается точкой**
- **Максимум 72 символа**
- **Описывает что именно сделано, а не зачем**

### Правильно:
```
feat(bot): добавить команду для просмотра источников
fix(db): исправить утечку памяти в пуле соединений
perf(cache): ускорить проверку дубликатов новостей
```

### Неправильно:
```
Добавлена новая команда
Исправлена ошибка
Улучшена производительность
```

## Подробное описание (опционально)

- **Отделяется пустой строкой** от заголовка
- **Подробно объясняет изменения**
- **Может содержать несколько абзацев**
- **Указывает на причины и следствия изменений**

```
fix(scraper): исправить обработку null байтов

Добавлена функция removeNullBytes() для очистки текстовых данных
от null байтов перед сохранением в базу данных.

Это предотвращает ошибки PostgreSQL "invalid byte sequence for encoding UTF8"
при сохранении контента новостей с бинарными данными.

Closes #123
```

## Ссылки на задачи (опционально)

- **Closes #123** - закрывает задачу
- **Fixes #456** - исправляет баг
- **Related to #789** - связано с задачей
- **BREAKING CHANGE:** - для breaking changes

## Breaking changes

Для изменений, ломающих обратную совместимость:

```
feat(api)!: изменить формат ответа API

BREAKING CHANGE: удален параметр `legacy_format`
Используйте новый параметр `format=v2`
```

## Примеры полных сообщений

### Новая функциональность
```
feat(redis): добавить кэширование контента новостей

Реализован Redis кэш для скрапированного контента с TTL 30 минут.
Повторные запросы к одним URL обслуживаются из кэша за 1-5мс
вместо 2-15сек, что дает ускорение 3-10x.

- Добавлена структура CachedNewsContent
- Интегрирован ContentCache в ContentScraper
- Добавлены тесты производительности
- Graceful degradation при недоступности Redis
```

### Исправление ошибки
```
fix(db): исправить сохранение контента с null байтами

Проблема: PostgreSQL отклоняет данные с null байтами (0x00)
в кодировке UTF8 с ошибкой "invalid byte sequence".

Решение: добавлена очистка null байтов на уровне скрапинга
и перед сохранением в базу данных.

- Добавлена функция removeNullBytes()
- Очистка применяется ко всем текстовым полям
- Добавлены unit-тесты для проверки
```

### Рефакторинг
```
refactor(bot): упростить логику обработки команд

Выделена общая логика обработки callback-запросов
в отдельные функции для улучшения читаемости кода.

- Добавлена функция handleCallbackQuery()
- Упрощена функция handleUpdate()
- Улучшена обработка ошибок
```

### Тесты
```
test(scraper): добавить тесты для обработки null байтов

Добавлены comprehensive unit-тесты для функций очистки текста:

- TestRemoveNullBytes: проверка удаления null байтов
- TestCleanUTF8String: проверка очистки UTF-8 строк
- Benchmark тесты производительности

Покрытие тестами увеличено с 49.2% до 52.1%
```

## Автоматизация

Проект включает Git hooks для автоматической проверки формата коммитов:

```bash
# Установка pre-commit hooks
./scripts/setup-hooks.sh

# Или вручную
cp scripts/pre-commit .git/hooks/
chmod +x .git/hooks/pre-commit
```

## Инструменты

### Проверка формата
```bash
# Проверка последнего коммита
./scripts/check-commit.sh HEAD

# Проверка диапазона коммитов
./scripts/check-commit.sh HEAD~5..HEAD
```

### Автоматическая генерация changelog
```bash
# Генерация changelog из conventional commits
npm install -g conventional-changelog-cli
conventional-changelog -p angular -i CHANGELOG.md -s
```

## Важные правила

1. **Всегда используйте тип из списка** - не придумывайте новые типы
2. **Указывайте область** - это помогает в навигации по истории
3. **Пишите на русском** - для consistency в команде
4. **Будьте конкретны** - описание должно четко показывать что изменилось
5. **Используйте imperative mood** - "добавить", "исправить", не "добавлен", "исправлен"
6. **Один коммит - одно изменение** - не смешивайте разные типы изменений

## Шаблоны для быстрого старта

### Быстрые команды:
```bash
# Фича
git commit -m "feat(bot): добавить поддержку голосовых сообщений"

# Исправление
git commit -m "fix(db): исправить deadlock в транзакциях"

# Документация
git commit -m "docs(readme): добавить раздел по мониторингу"

# Тесты
git commit -m "test(api): добавить интеграционные тесты"

# Производительность
git commit -m "perf(cache): оптимизировать алгоритм LRU"
```

### Использование с Git:
```bash
# Интерактивный коммит с шаблоном
git commit -t .gitmessage

# Правка последнего коммита
git commit --amend -m "fix(scraper): исправить обработку ошибок сети"
```

## Проверка качества

Сообщения коммитов проверяются на:
- ✅ Соответствие формату Conventional Commits
- ✅ Наличие типа и области
- ✅ Длину заголовка (не более 72 символов)
- ✅ Отсутствие лишних точек и заглавных букв
- ✅ Наличие подробного описания для сложных изменений

**Следуйте этим правилам для поддержания качества и читаемости истории проекта!** 📝✅
