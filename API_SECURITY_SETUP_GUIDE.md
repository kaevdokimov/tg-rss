# Итоговая инструкция: Настройка API Security для News Analyzer

## 📋 Выполненные задачи

### 1. ✅ Исправлена ошибка Permission denied
- **Проблема:** `/health` возвращал `[Errno 13] Permission denied: 'environments'`
- **Решение:** Исправлен путь к директории `environments` в `src/config/validator.py`

### 2. ✅ Добавлена защита документации паролем
- **Что защищено:** `/docs`, `/redoc`, `/openapi.json`
- **Метод:** HTTP Basic Authentication
- **Конфигурация:** Через переменные окружения

### 3. ✅ Настроена интеграция с GitHub Actions
- Обновлен CI/CD pipeline для передачи secrets
- Обновлен docker-compose.yml
- Созданы инструкции по настройке

## 🔐 Настройка GitHub Secrets

### Шаг 1: Добавление Secrets в GitHub

#### Вариант А: Через веб-интерфейс

1. Откройте https://github.com/YOUR_USERNAME/YOUR_REPO/settings/secrets/actions
2. Нажмите **New repository secret**
3. Добавьте первый секрет:
   - **Name:** `NEWS_ANALYZER_ADMIN`
   - **Value:** `admin` (или другое имя пользователя)
4. Добавьте второй секрет:
   - **Name:** `NEWS_ANALYZER_PASSWORD`
   - **Value:** сгенерируйте надежный пароль (см. ниже)

#### Вариант Б: Через GitHub CLI

```bash
# Установите gh CLI если не установлен
brew install gh  # macOS
# или
sudo apt install gh  # Ubuntu

# Авторизуйтесь
gh auth login

# Добавьте secrets
gh secret set NEWS_ANALYZER_ADMIN -b"admin"
gh secret set NEWS_ANALYZER_PASSWORD -b"$(openssl rand -base64 32)"
```

### Шаг 2: Генерация надежного пароля

```bash
# Способ 1: OpenSSL (32 символа)
openssl rand -base64 32

# Способ 2: pwgen (если установлен)
pwgen -s 32 1

# Способ 3: Python
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

**⚠️ Важно:** Сохраните сгенерированный пароль в надежном месте (например, password manager)!

### Шаг 3: Проверка добавленных Secrets

```bash
# Через GitHub CLI
gh secret list | grep NEWS_ANALYZER

# Вывод должен быть:
# NEWS_ANALYZER_ADMIN      Updated 2026-01-25
# NEWS_ANALYZER_PASSWORD   Updated 2026-01-25
```

## 🚀 Деплой изменений

### Автоматический деплой (рекомендуется)

После добавления secrets в GitHub:

```bash
# 1. Push коммитов в main branch
cd /Users/kirillevdokimov/GolandProjects/tg-rss
git push origin main

# 2. GitHub Actions автоматически:
#    - Соберет Docker образы
#    - Задеплоит на сервер
#    - Передаст secrets как переменные окружения
```

### Ручной деплой (если нужно)

```bash
# На сервере
cd ~/news-bot

# Обновить .env файл вручную
echo "NEWS_ANALYZER_ADMIN=admin" >> .env
echo "NEWS_ANALYZER_PASSWORD=your_password_here" >> .env

# Перезапустить контейнер
docker-compose up -d --force-recreate news-analyzer
```

## ✅ Проверка работы

### 1. Проверка через curl

```bash
# Замените your-server на ваш домен/IP и порт
SERVER="your-server:8000"

# Тест 1: Открытый endpoint (должен работать)
curl -s http://$SERVER/health | jq .

# Тест 2: Защищенный endpoint без авторизации (должен вернуть 401)
curl -s http://$SERVER/docs
# Ожидаемый результат: {"detail":"Not authenticated"}

# Тест 3: Защищенный endpoint с неправильным паролем (должен вернуть 401)
curl -s -u admin:wrong http://$SERVER/docs
# Ожидаемый результат: {"detail":"Неверные учетные данные"}

# Тест 4: Защищенный endpoint с правильным паролем (должен вернуть HTML)
curl -s -u admin:your_password http://$SERVER/docs | head -10
# Ожидаемый результат: <!DOCTYPE html>...
```

### 2. Проверка через браузер

1. Откройте `http://your-server:8000/docs`
2. Браузер должен показать окно авторизации
3. Введите:
   - **Username:** значение из `NEWS_ANALYZER_ADMIN`
   - **Password:** значение из `NEWS_ANALYZER_PASSWORD`
4. После успешной авторизации откроется Swagger UI

### 3. Проверка логов контейнера

```bash
# На сервере
docker logs news-analyzer --tail 50

# Ищите строки вроде:
# INFO:     Uvicorn running on http://0.0.0.0:8000
# INFO:     Application startup complete
```

### 4. Проверка переменных окружения в контейнере

```bash
# На сервере
docker exec news-analyzer env | grep NEWS_ANALYZER

# Вывод должен быть:
# NEWS_ANALYZER_ADMIN=admin
# NEWS_ANALYZER_PASSWORD=your_password_here
```

## 📁 Структура файлов

```
tg-rss/
├── .env                              # Локальная конфигурация (добавлены переменные)
├── env.example                       # Пример конфигурации (добавлены переменные)
├── docker-compose.yml                # Docker конфигурация (добавлены environment vars)
├── SECRETS_SETUP.md                  # Инструкция по настройке secrets (новый файл)
├── .github/workflows/
│   └── ci-cd.yml                     # CI/CD pipeline (обновлен с secrets)
└── news-analyzer-python/
    ├── .env                          # Локальная конфигурация для разработки
    ├── env.example                   # Пример конфигурации (обновлен)
    ├── API_SECURITY_README.md        # Техническая документация (новый файл)
    └── src/
        ├── config/
        │   └── validator.py          # Исправлен путь к environments
        └── monitoring/
            └── api.py                # Добавлена Basic Auth защита
```

## 🔄 Обновление паролей

### Когда обновлять:
- Регулярно (рекомендуется каждые 90 дней)
- При подозрении на компрометацию
- При смене команды/персонала

### Как обновить:

```bash
# 1. Генерируем новый пароль
NEW_PASSWORD=$(openssl rand -base64 32)
echo "Новый пароль: $NEW_PASSWORD"

# 2. Обновляем в GitHub
gh secret set NEWS_ANALYZER_PASSWORD -b"$NEW_PASSWORD"

# 3. Триггерим редеплой
git commit --allow-empty -m "chore: обновить api password"
git push origin main

# 4. Проверяем что пароль обновился
# После завершения деплоя попробуйте войти с новым паролем
```

## 🐛 Troubleshooting

### Проблема: 401 даже с правильным паролем

**Причины и решения:**

1. **Secrets не обновились на сервере**
   ```bash
   # Проверить переменные в контейнере
   docker exec news-analyzer env | grep NEWS_ANALYZER
   
   # Если пусто - пересоздать контейнер
   docker-compose up -d --force-recreate news-analyzer
   ```

2. **Используется кэшированный старый образ**
   ```bash
   # Удалить старый образ и пересобрать
   docker rmi ghcr.io/YOUR_USERNAME/news-analyzer:latest
   docker-compose pull news-analyzer
   docker-compose up -d news-analyzer
   ```

3. **Пароль содержит спецсимволы**
   ```bash
   # В curl нужно экранировать или кодировать
   curl -u "admin:password@123" http://server:8000/docs  # Неправильно
   curl -u "admin" -H "Authorization: Basic $(echo -n 'admin:password@123' | base64)" http://server:8000/docs  # Правильно
   ```

### Проблема: Контейнер не запускается

```bash
# Проверить логи
docker logs news-analyzer --tail 100

# Проверить что .env файл существует и читаем
docker exec news-analyzer cat /app/.env 2>/dev/null || echo ".env не найден в контейнере"

# Проверить что uvicorn запущен
docker exec news-analyzer ps aux | grep uvicorn
```

### Проблема: GitHub Actions не видит secrets

**Решение:**
1. Убедитесь что secrets добавлены в **Repository secrets** (не Environment secrets)
2. Проверьте права доступа - нужны права администратора
3. Проверьте что workflow файл использует правильные имена secrets

## 📚 Полезные ссылки

- **Техническая документация:** `news-analyzer-python/API_SECURITY_README.md`
- **Инструкция по secrets:** `SECRETS_SETUP.md`
- **FastAPI Security:** https://fastapi.tiangolo.com/tutorial/security/
- **GitHub Secrets:** https://docs.github.com/en/actions/security-guides/encrypted-secrets

## ✅ Checklist финальной проверки

- [ ] Secrets добавлены в GitHub
- [ ] Изменения закоммичены и запушены
- [ ] GitHub Actions успешно завершился
- [ ] Контейнер news-analyzer работает (`docker ps | grep news-analyzer`)
- [ ] `/health` возвращает статус (без ошибки Permission denied)
- [ ] `/docs` требует авторизацию (401 без пароля)
- [ ] `/docs` открывается с правильным паролем
- [ ] Пароль надежный (не `changeme`)
- [ ] Пароль сохранен в password manager

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Документация API защищена паролем
- ✅ Ошибка Permission denied исправлена
- ✅ Secrets настроены и передаются через CI/CD
- ✅ Приложение готово к production использованию

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте раздел Troubleshooting выше
2. Изучите логи: `docker logs news-analyzer`
3. Проверьте GitHub Actions: https://github.com/YOUR_REPO/actions
