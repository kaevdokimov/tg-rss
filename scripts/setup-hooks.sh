#!/bin/bash

# Скрипт для установки Git hooks для проверки conventional commits

set -e

HOOKS_DIR=".git/hooks"
PRE_COMMIT_HOOK="$HOOKS_DIR/pre-commit"

# Проверяем что мы в git репозитории
if [ ! -d ".git" ]; then
    echo "❌ Ошибка: скрипт должен выполняться из корня git репозитория"
    exit 1
fi

echo "🔧 Установка Git hooks для проверки conventional commits..."

# Создаем директорию hooks если не существует
mkdir -p "$HOOKS_DIR"

# Создаем pre-commit hook
cat > "$PRE_COMMIT_HOOK" << 'EOF'
#!/bin/bash

# Pre-commit hook для проверки conventional commits
# Автоматически проверяет сообщения коммитов перед коммитом

# Получаем сообщение коммита
COMMIT_MSG_FILE=$1

if [ -z "$COMMIT_MSG_FILE" ]; then
    echo "❌ Ошибка: не найден файл сообщения коммита"
    exit 1
fi

# Читаем сообщение коммита
commit_msg=$(head -1 "$COMMIT_MSG_FILE")

echo "🔍 Проверка сообщения коммита: \"$commit_msg\""

# Проверка формата: тип(область): описание
if ! echo "$commit_msg" | grep -qE '^(feat|fix|docs|style|refactor|test|chore|perf|ci|build)(\([a-zA-Z0-9_-]+\))?: '; then
    echo "❌ Ошибка: неверный формат сообщения коммита"
    echo "   Ожидается: тип(область): описание"
    echo "   Примеры:"
    echo "   • feat(bot): добавить поддержку inline-кнопок"
    echo "   • fix(db): исправить утечку null байтов"
    echo "   • perf(cache): ускорить поиск дубликатов"
    echo ""
    echo "📖 Подробности: https://github.com/kaevdokimov/tg-rss/blob/main/COMMIT_CONVENTION.md"
    exit 1
fi

# Проверка длины (не более 72 символов)
if [ ${#commit_msg} -gt 72 ]; then
    echo "❌ Ошибка: заголовок коммита слишком длинный (${#commit_msg} символов, максимум 72)"
    exit 1
fi

# Проверка что не заканчивается точкой
if echo "$commit_msg" | grep -q '\.$'; then
    echo "❌ Ошибка: описание не должно заканчиваться точкой"
    exit 1
fi

# Проверка что описание начинается с маленькой буквы
description=$(echo "$commit_msg" | sed 's/^[^(]*([^)]*): //' 2>/dev/null || echo "$commit_msg")
if [[ $description =~ ^[А-ЯA-Z] ]]; then
    echo "❌ Ошибка: описание должно начинаться с маленькой буквы"
    exit 1
fi

echo "✅ Сообщение коммита соответствует соглашению"
exit 0
EOF

# Делаем hook исполняемым
chmod +x "$PRE_COMMIT_HOOK"

echo "✅ Pre-commit hook установлен успешно!"
echo ""
echo "📝 Теперь все коммиты будут автоматически проверяться на соответствие"
echo "   соглашению conventional commits."
echo ""
echo "💡 Если нужно пропустить проверку (например, для hotfix):"
echo "   git commit --no-verify -m \"fix: быстрая правка\""
echo ""
echo "📖 Подробности: COMMIT_CONVENTION.md"
